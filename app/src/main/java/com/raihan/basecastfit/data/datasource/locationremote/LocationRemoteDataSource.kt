package com.raihan.basecastfit.data.datasource.locationremote

import com.raihan.basecastfit.data.source.network.model.weather.WeatherData
import com.raihan.basecastfit.data.source.network.service.CastFitApiService

/*
interface LocationRemoteDataSource {

    suspend fun getWeatherData(
        key : String,
        query: String
    ) : WeatherData
}

class LocationRemoteDataSourceImpl(private val service: CastFitApiService) : LocationRemoteDataSource {
    override suspend fun getWeatherData(
        key: String,
        query: String
    ): WeatherData {
        return service.getWeatherData(
            key = key,
            query = query
        )
    }
}*/
